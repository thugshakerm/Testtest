# Required: password used by the PostgreSQL container.
DB_PASSWORD=change-this-password

# Optional: hostname used in legacy game/asset URLs. Do not include https://.
# Leave as localhost:9000 for a local-only test.
SITE_DOMAIN=localhost:9000

# Optional: set true to skip the build and only start containers.
SKIP_BUILD=false
